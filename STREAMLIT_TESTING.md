# تست نسخه Streamlit

Branch:

`agent/streamlit-user-prototype`

## اجرا

```bash
python -m pip install -r requirements.txt
streamlit run streamlit_app.py
```

بعد آدرس محلی Streamlit را باز کنید (معمولاً `http://localhost:8501`).

## چیزهایی که باید تست شوند

1. صفحه اول باید فهرست گروه‌بندی‌شده حدود ۴۰ غذا را نشان دهد.
2. کاربر چند غذای معمول خود را انتخاب و ذخیره می‌کند.
3. در صفحه اصلی باید بتواند زمان، هزینه، سبک/سنگین و دسته غذا را هم‌زمان انتخاب کند.
4. زمان‌های بالای ۶۰ دقیقه باید به شکل «۱ ساعت و ... دقیقه» نمایش داده شوند.
5. نتیجه باید زمان آماده‌سازی، زمان پخت و زمان کل را جدا نشان دهد.
6. غذاهای معمول کاربر باید اولویت بالاتری داشته باشند.
7. کاربر می‌تواند چند غذای جدید برای امتحان انتخاب کند و آن‌ها فقط گاهی وارد پیشنهاد شوند.
8. دسته‌بندی‌های اصلی شامل پلو و چلو، خورش، آش و سوپ، غذای نانی و خوراک، کوکو/کتلت/تخم‌مرغی، پاستا و ساندویچ/فست‌فود خانگی هستند.

کالری، قیمت دقیق و تشخیص خرید بر اساس موجودی خانه عمداً برای مرحله بعد نگه داشته شده‌اند.
