import{createServer}from'node:http';import{readFile,stat}from'node:fs/promises';import{extname,resolve,sep}from'node:path';
const root=process.cwd(),port=Number(process.env.PORT||4173),host=process.env.HOST||'0.0.0.0';
const types={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8','.mjs':'text/javascript; charset=utf-8','.json':'application/json; charset=utf-8','.webmanifest':'application/manifest+json; charset=utf-8','.png':'image/png','.svg':'image/svg+xml'};
function send(res,status,body,type='text/plain; charset=utf-8'){res.writeHead(status,{'content-type':type,'cache-control':'no-store'});res.end(body)}
createServer(async(req,res)=>{try{const url=new URL(req.url,'http://localhost'),raw=decodeURIComponent(url.pathname);let relative=raw.replace(/^\/+/, '');if(!relative||relative.endsWith('/'))relative+='index.html';const file=resolve(root,relative);if(file!==root&&!file.startsWith(root+sep)){send(res,403,'Forbidden');return}const info=await stat(file);if(!info.isFile()){send(res,404,'Not found');return}const body=await readFile(file);send(res,200,body,types[extname(file)]||'application/octet-stream')}catch(error){if(error?.code==='ENOENT')send(res,404,'Not found');else{console.error(error);send(res,500,'Server error')}}}).listen(port,host,()=>console.log(`Food Choice preview: http://localhost:${port}`));
