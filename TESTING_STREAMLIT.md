# تست نسخه کاربرمحور Food Choice

Branch: `agent/streamlit-user-prototype`

## ساده‌ترین روش تست — بدون نصب

فقط این لینک را باز کنید. StackBlitz پروژه را از GitHub می‌گیرد و نسخه مرورگری تست را اجرا می‌کند:

`https://stackblitz.com/github/Shahbazi-Amir/Food_choice/tree/agent/streamlit-user-prototype?startScript=serve&embed=1&view=preview`

هیچ Python، Streamlit، npm package یا نصب محلی لازم نیست.

نسخه مرورگری برای تست تجربه کاربر ساخته شده و همان ایده‌های نسخه Streamlit را دارد: انتخاب غذاهای معمول، غذاهای جدید برای امتحان، فیلتر هم‌زمان زمان/هزینه/سبکی/دسته‌بندی و نمایش زمان آماده‌سازی + پخت + مجموع.

## نسخه Streamlit واقعی برای توسعه

اگر توسعه‌دهنده خواست نسخه Python را اجرا کند:

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

این بخش برای کاربر نهایی لازم نیست.
