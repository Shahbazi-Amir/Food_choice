# تست نسخه آزمایشی Food Choice 0.2

این شاخه برای تست قبل از merge به `main` ساخته شده است:

`agent/testable-mvp-fixes`

## اجرای سریع

بدون نصب dependency:

```bash
npm test
npm run serve
```

بعد آدرس زیر را باز کنید:

`http://localhost:4173`

برای تست آنلاین و دیدن مستقیم خود اپ، همین branch را در StackBlitz Preview باز کنید:

`https://stackblitz.com/github/Shahbazi-Amir/Food_choice/tree/agent/testable-mvp-fixes?startScript=serve&embed=1&view=preview`

اگر Preview خودکار اجرا نشد، نسخه کامل StackBlitz را باز کنید:

`https://stackblitz.com/github/Shahbazi-Amir/Food_choice/tree/agent/testable-mvp-fixes?startScript=serve`

و در Terminal دستور زیر را اجرا کنید:

```bash
npm run serve
```

## چک‌لیست تست کاربر

1. بار اول باید سؤال «معمولاً برای چند نفر غذا می‌پزی؟» دیده شود.
2. یک تعداد نفر انتخاب کنید و صفحه را Refresh کنید؛ تعداد باید حفظ شود.
3. «یک غذا انتخاب کن» را بزنید؛ نام غذا، زمان و مواد لازم باید ظاهر شوند.
4. تعداد نفرات را کم و زیاد کنید؛ مقدار مواد باید همان لحظه تغییر کند و صفحه بی‌دلیل اسکرول نکند.
5. «یکی دیگر» را چند بار بزنید؛ همان پیشنهاد فعلی نباید فوراً دوباره برگردد.
6. حالت‌های سریع، متعادل‌تر و اقتصادی را امتحان کنید.
7. «دستور پخت را نشان بده» باید مراحل دستور را باز و بسته کند.
8. «این را می‌پزم» را بزنید؛ فقط یک بار در تاریخچه ثبت شود حتی اگر سریع دوبار لمس شود.
9. صفحه را Refresh کنید؛ تاریخچه باید باقی بماند.
10. بخش «تعادل این هفته» باید بر اساس غذاهای ثبت‌شده تغییر کند.
11. دکمه نصب را بزنید؛ روی Android باید prompt نصب یا راهنما نمایش داده شود و روی iPhone راهنمای Add to Home Screen نشان داده شود.
12. بعد از یک بار باز شدن آنلاین، اینترنت را قطع و صفحه را دوباره باز کنید؛ shell اصلی باید از cache باز شود.
13. پایین صفحه وضعیت «آنلاین/آفلاین» باید درست تغییر کند.

## اصلاحات این نسخه

- جلوگیری از ثبت تکراری غذا با دو لمس سریع
- جلوگیری از اسکرول ناخواسته هنگام تغییر تعداد نفرات
- fallback به Local Storage اگر IndexedDB در مرورگر در دسترس نباشد
- راهنمای نصب برای iPhone و مرورگرهایی که prompt نصب مستقیم ندارند
- Service Worker امن‌تر و cache نسخه‌دار
- افزودن metadata مناسب iOS/PWA
- سرور تست بدون dependency و بدون Python
- تست DOM contract، manifest و iconها
- تست‌های بیشتر برای منطق پیشنهاد غذا
- smoke test واقعی HTTP در GitHub Actions
